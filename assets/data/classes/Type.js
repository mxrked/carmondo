export default class Type {
  constructor(typeID, typeName, typeBg, typeLink) {
    this._typeID = typeID;
    this._typeName = typeName;
    this._typeBg = typeBg;
    this._typeLink = typeLink;
  }
}
